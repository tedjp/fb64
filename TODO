- Fix integer overflow for buffer lengths of >⅓SIZE_MAX
- Add tests for invalid input
- Rename buflen functions to decoded_len/decoded_size
- Add encoder
- More benchmarks (eg. coreutils)
- Benchmark "real" encoded data (eg. lots of zero bits).
